import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.voiceFinder.app',
  appName: 'Voice-Finder',
  webDir: 'src',
  bundledWebRuntime: false
};

export default config;
